#include "types.h"

// Namespace inferred from existing PS2 symbol/file clusters.

namespace YS {

} // namespace YS

class TEST_SCALE;

class TEST_SCALE {
public:
 void Open(s32 a0);
 void Init(void);
 ~TEST_SCALE();
};
